# Dependencies
node_modules

# yarn error logs
yarn-error.log

# Environment varibales
.env*
!.env*.example

# Code coverage
coverage

# Test run output (local/CI scratch — do not commit)
test-full*.log
.test-full-output.txt
*.test-output.log

# Local webhook payload backups (LiveKit Egress)
recordings/

# agent / session context (local)
.context/

# local / generated â€” do not commit
docs/
# Local ops scripts (never push)
scripts/*
!scripts/mirror-cloud-to-local.js
# test runner + manifest must ship so the tracked tests are runnable on a clone
!scripts/run-tests.mjs
!scripts/test-manifest.json
tests/
graphify-out/

# tests stay local â€” never pushed
**/__tests__/
*.test.js
*.test.mjs

# Notification Batch 1 tests ARE tracked (registered in scripts/test-manifest.json).
# Parent dirs must be re-included first: git cannot re-include a file whose
# parent directory is excluded. Everything else under them stays ignored.
!src/__tests__/
!src/__tests__/routes/
!src/__tests__/routes/notification.route.permissions.test.js
!src/services/__tests__/
src/services/__tests__/fixtures/
src/services/__tests__/helpers/
!src/services/__tests__/job.applicationNotification.test.js
!src/services/meeting.service.test.js
!src/services/offer.service.test.js

# Meeting reminder repair (2026-09-08) — these guard a production delivery failure.
# Parent dirs re-included first (git cannot re-include a file whose parent dir is excluded).
!src/models/__tests__/
!src/models/__tests__/notification.relatedEntity.test.js
!src/services/__tests__/notification.channelIsolation.test.js
!src/services/__tests__/reminderDispatcher.test.js
!src/services/__tests__/meeting.conclusionRecipients.test.js
!src/services/__tests__/meeting.reminders.test.js
!src/services/__tests__/internalMeeting.reminders.test.js
!src/services/__tests__/meeting.rescheduleResetsReminder.test.js
!src/services/__tests__/meetingIcs.test.js
!src/services/__tests__/meetingSeries.inviteOnAdd.test.js
!scripts/migrations/
scripts/migrations/*
!scripts/migrations/__tests__/
!scripts/migrations/__tests__/2026-09-08-backfill-meeting-reminder-retry.test.js
!scripts/migrations/2026-09-08-backfill-meeting-reminder-retry.js

# editor plans and CI config stay local â€” never pushed
.cursor/
.github/

AGENTS.md
postman/

# local scratch / exports (do not commit)
referral_hits.csv

# One-off dev-ticket / local ops scripts (never push)
src/scripts/backfillDevTicket*.js
src/scripts/smokeDevTicketEmailFanout.js
src/scripts/dev-ticket-email-preview.html
src/scripts/.patchTwilioCallGaps.backup.json
src/scripts/.removedEc2Notes.backup.json
src/scripts/inspectRecruitmentData.js
src/scripts/queryRafiqulOffers.js
src/scripts/recoverDevTicketsFromPdf.js
src/scripts/repairCandidateApplicantState.js
src/scripts/cleanupMisownedCandidates.js

# Deletion backups â€” contain real names/emails/phones, never commit
backups/


# Local AI tool config (do not commit)
.claude/
.codex/
CLAUDE.md
.impeccable/

# Backfill script run journals (carry --undo paths, not source)
attendance-status-backfill.*.journal.json

# Compensation snapshot guard: a bulk form PATCH must never revert an offer-derived value.
!src/services/__tests__/candidateCompensationWrite.test.js
!src/services/__tests__/offerCompensationSync.test.js
!src/utils/__tests__/
!src/utils/__tests__/auditMetadata.helper.test.js
!src/services/__tests__/offerCompensationGate.test.js

# Backdated attendance day-collapse (2026-09-10) — guards a production data-corruption bug
# (approve left leftover punch sessions on the day, double-counting the calendar total).
# Parent dirs are re-included above; these two files ship, the rest of the dirs stay ignored.
!src/services/__tests__/backdatedAttendanceRequest.approve.test.js
!scripts/migrations/2026-09-10-collapse-backdated-duplicate-days.js
!scripts/migrations/2026-09-10-backfill-document-versions.js
!scripts/migrations/__tests__/2026-09-10-backfill-document-versions.test.js
