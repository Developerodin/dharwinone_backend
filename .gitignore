# Dependencies
node_modules

# yarn error logs
yarn-error.log

# Environment varibales
.env*
!.env*.example

# Code coverage
coverage

# Local webhook payload backups (LiveKit Egress)
recordings/

# agent / session context (local)
.context/

# local / generated — do not commit
docs/
# scripts/* not scripts/: git cannot re-include a file whose parent dir is excluded
scripts/*
!scripts/mirror-cloud-to-local.js
tests/
graphify-out/

# tests stay local — never pushed
**/__tests__/
*.test.js
*.test.mjs

AGENTS.md
postman/

# local scratch / exports (do not commit)
referral_hits.csv


# Local AI tool config (do not commit)
.claude/
.codex/
